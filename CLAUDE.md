# 작업 방식 (이 저장소의 기본 규칙)

## 1. 단순 작업은 바로, 복잡한 작업은 착수 전 1회 확인
- **단순** (오타·문구 한 줄·값 하나·명확한 단일 수정): 묻지 말고 바로 편집한다.
- **복잡** (여러 단계·여러 파일이 엮이거나, 범위가 모호하거나, 되돌리기 어렵거나, 방향 선택지가 갈릴 때): 착수 **전에** "이 범위/방향 맞냐"를 짧게 1회 확인하고 시작한다.
- 확인은 시작 전 1회만. 작업 도중 매 단계 묻지 않는다. 동의되면 쭉 진행한다.

## 2. 검증은 명시 요청 시에만
- 검증/감사/빌드/Lighthouse/수정루프를 **사용자가 요청하지 않으면 붙이지 않는다.** 선택지로 먼저 제안하지도 않는다.
- 빌드(`lms-build`) 기본 단계는 `['strategy','design','production']` — QA 미포함. "감사해줘"라고 명시할 때만 `phases`에 `'qa'`를 추가한다.
- 착수 전 확인(규칙 1)과 검증 프로세스는 별개다. 묻는 것이 곧 검증을 붙이라는 뜻이 아니다.
- 의심되면 과소 검증을 택한다.
